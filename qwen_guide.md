# Qwen Integration Guide

## 🎯 Why Qwen?

Qwen (Tongyi Qianwen) от Alibaba - отличный выбор для русскоязычных собеседований:

### Преимущества Qwen:
- ✅ **Отличная поддержка русского языка**
- ✅ **Хорошее понимание технических вопросов**
- ✅ **Оптимизирован для программирования**
- ✅ **Бесплатная локальная версия через Ollama**
- ✅ **Официальный API доступен**

## 🚀 Установка Qwen

### Вариант 1: Локальная установка (рекомендуется)

```bash
# Установи Ollama если еще не установлен
# https://ollama.com/

# Выбери подходящую модель:
ollama pull qwen2.5:3b    # Легкая (~2GB) - для быстрой работы
ollama pull qwen2.5:7b    # Баланс (~5GB) - рекомендуется
ollama pull qwen2.5:14b   # Мощная (~9GB) - максимальное качество
```

### Вариант 2: Облачный API

```bash
# Получи API ключ от Alibaba Cloud
# https://www.alibabacloud.com/product/tongyi

# Установи переменную окружения
set QWEN_API_KEY=your-api-key-here
```

## 📊 Рекомендации для Ryzen 5700U

| Модель | Размер | RAM | Производительность | Рекомендация |
|--------|--------|-----|-------------------|--------------|
| qwen2.5:0.5b | ~0.5GB | 2GB | Очень быстрая | Для тестов |
| qwen2.5:1.5b | ~1GB | 3GB | Быстрая | Хороший выбор |
| qwen2.5:3b | ~2GB | 4GB | Баланс | **РЕКОМЕНДУЕТСЯ** |
| qwen2.5:7b | ~5GB | 6GB | Высокая | **ОПТИМАЛЬНО** |
| qwen2.5:14b | ~9GB | 10GB | Максимальная | Если много RAM |

**Для твоего Ryzen 5700U + 16GB: qwen2.5:7b идеальный выбор!**

## 🔧 Конфигурация

В `config.py` уже настроено:

```python
LLM_MODEL_NAME = "qwen2.5:7b"  # Используем Qwen
USE_CLOUD_LLM = False          # Локальная версия
```

Для облачной версии:
```python
USE_CLOUD_LLM = True
# И установи QWEN_API_KEY в переменных окружения
```

## 🧪 Тестирование

### Проверка доступных моделей:
```bash
python test_qwen_integration.py
```

### Быстрый тест Qwen:
```bash
python -c "from qwen_handler import QwenHandler; h = QwenHandler(); print(h.generate_response('Что такое React?'))"
```

## 🎯 Примеры работы

**Вопрос:** "В чем разница между interface и type в TypeScript?"

**Ответ Qwen:**
```
• Interface: расширяемый (можно объединять), лучше для объектов и классов
• Type: для примитивов, union типов, кортежей
• Interface поддерживает merging, Type - нет
• Для библиотек лучше Interface, для внутреннего кода - Type
```

## ⚡ Оптимизация производительности

### Для максимальной скорости:
```python
# config.py
WHISPER_MODEL_SIZE = "tiny"  # Быстрее STT
LLM_MODEL_NAME = "qwen2.5:3b"  # Легче модель
```

### Для максимального качества:
```python
# config.py
WHISPER_MODEL_SIZE = "small"  # Точнее STT
LLM_MODEL_NAME = "qwen2.5:14b"  # Умнее модель
```

## 🐛 Решение проблем

### Модель не найдена:
```bash
ollama list  # Проверить установленные модели
ollama pull qwen2.5:7b  # Установить нужную модель
```

### Медленная генерация:
- Закрой другие приложения
- Используй более легкую модель (3b вместо 7b)
- Убедись что достаточно RAM

### Ошибки API:
- Проверь интернет соединение
- Убедись в правильности API ключа
- Проверь квоты на облачном аккаунте

## 📈 Сравнение с другими моделями

| Модель | Русский язык | Технические вопросы | Скорость | Размер |
|--------|-------------|-------------------|----------|---------|
| Qwen 7b | ✅ Отлично | ✅ Отлично | ⚡ Быстро | 📦 Средний |
| Llama 3.2 | ⚠️ Хорошо | ⚠️ Хорошо | ⚡ Быстро | 📦 Средний |
| Phi-3 | ⚠️ Средне | ⚠️ Средне | ⚡ Очень быстро | 📦 Легкий |

**Qwen побеждает по качеству русского языка и технической экспертизе!**

## 🚀 Готово к использованию!

После установки модели просто запусти:
```bash
python ghost_gui.py
```

И получишь профессионального помощника для собеседований с Qwen! 🎯