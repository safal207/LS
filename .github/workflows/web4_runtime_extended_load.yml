name: Web4 Runtime Extended Load

on:
  workflow_dispatch:
    inputs:
      phase4_duration_s:
        description: "Phase 4 soak duration in seconds"
        required: false
        default: "120"
      sessions:
        description: "Number of sessions for phase3/phase4 runs"
        required: false
        default: "20"
      target_messages_per_s:
        description: "Target aggregate throughput for phase4"
        required: false
        default: "200"
  schedule:
    - cron: "30 3 * * *"
  pull_request:
    branches:
      - "main"
    paths:
      - "python/modules/web4_runtime/**"
      - "python/tests/test_web4_runtime_loadtest.py"
      - "python/tests/test_fuzzy_runtime.py"
      - "scripts/web4_runtime_load.py"
      - ".github/workflows/web4_runtime_extended_load.yml"

jobs:
  phase3-chaos:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    env:
      RUN_SESSIONS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.sessions || '20' }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest

      - name: Run phase3 chaos profile
        run: |
          PYTHONPATH=python:python/modules python scripts/web4_runtime_load.py \
            --phase phase3 \
            --mode both \
            --sessions "${RUN_SESSIONS}" \
            --messages-per-session 120 \
            --max-queue 64 \
            --total-limit 768 \
            --per-session-limit 64 \
            --chaos-disconnect-rate 0.12 \
            --burst-probability 0.2 \
            --burst-size 24 \
            --gc-pressure

  phase4-soak:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: phase3-chaos
    env:
      RUN_SESSIONS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.sessions || '20' }}
      PHASE4_DURATION_S: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.phase4_duration_s || '120' }}
      TARGET_MESSAGES_PER_S: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.target_messages_per_s || '200' }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest

      - name: Run phase4 soak profile
        run: |
          PYTHONPATH=python:python/modules python scripts/web4_runtime_load.py \
            --phase phase4 \
            --mode both \
            --sessions "${RUN_SESSIONS}" \
            --max-queue 64 \
            --total-limit 768 \
            --per-session-limit 64 \
            --soak-duration-s "${PHASE4_DURATION_S}" \
            --target-messages-per-s "${TARGET_MESSAGES_PER_S}" \
            --chaos-disconnect-rate 0.08 \
            --burst-probability 0.15 \
            --burst-size 16

  pr-load-smoke:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest

      - name: Run phase3 chaos smoke profile
        run: |
          PYTHONPATH=python:python/modules python scripts/web4_runtime_load.py \
            --phase phase3 \
            --mode both \
            --sessions 8 \
            --messages-per-session 60 \
            --max-queue 32 \
            --total-limit 256 \
            --per-session-limit 32 \
            --chaos-disconnect-rate 0.10 \
            --burst-probability 0.15 \
            --burst-size 12

      - name: Run phase4 soak smoke profile
        run: |
          PYTHONPATH=python:python/modules python scripts/web4_runtime_load.py \
            --phase phase4 \
            --mode both \
            --sessions 8 \
            --max-queue 32 \
            --total-limit 256 \
            --per-session-limit 32 \
            --soak-duration-s 20 \
            --target-messages-per-s 160 \
            --chaos-disconnect-rate 0.06 \
            --burst-probability 0.10 \
            --burst-size 10
