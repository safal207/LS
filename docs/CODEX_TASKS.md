# Codex Task List (auto-actionable backlog)

Этот документ содержит формализованные задачи, которые Codex может выполнять автоматически.
Задачи выведены из `docs/MAIN_BRANCH_STATUS_REPORT.md` и отсортированы по приоритету.

## Правила исполнения

- Брать задачи строго сверху вниз.
- Для каждой задачи открывать отдельный PR.
- После закрытия задачи обновлять статус в этом файле (`TODO` → `IN PROGRESS` → `DONE`).
- Любая задача считается выполненной только при зелёном CI.

---

## 🔥 PRIORITY 1 — Rust Core Stability

- [ ] **1.1 Добавить Rust unit-тесты** (`TODO`)
  - Область: `rust_core/src/memory_manager.rs`, `rust_core/src/pattern_matcher.rs`, `rust_core/src/storage.rs`, `rust_core/src/transport/*.rs`.
  - Критерии:
    - минимум 1 тест на модуль;
    - тесты запускаются через `cargo test`.

- [ ] **1.2 Включить cargo test в CI** (`TODO`)
  - Область: `.github/workflows/web4runtimeci.yml`.
  - Критерии:
    - добавлен шаг `cargo test --all --release`;
    - шаг обязателен для прохождения CI.

- [ ] **1.3 Устранить Rust warnings** (`TODO`)
  - Критерии:
    - исправлен `dead_code` для `max_size_mb`;
    - устранены предупреждения PyO3 (`non-local impl definition`).

---

## 🔥 PRIORITY 2 — Python CI Expansion

- [ ] **2.1 Расширить Python CI** (`TODO`)
  - Критерии:
    - в CI добавлены smoke-тесты из `python/tests/`;
    - добавлены unit-тесты `hexagon_core`.

- [ ] **2.2 Добавить тесты Python↔Rust bridge** (`TODO`)
  - Критерии:
    - проверяется импорт `ghostgpt_core`;
    - проверяется доступность всех экспортируемых классов;
    - проверяется корректность fallback-режима.

---

## 🔥 PRIORITY 3 — Improve Rust↔Python Integration

- [ ] **3.1 Добавить health-метрики в `rust_bridge.py`** (`TODO`)
  - Критерии:
    - логируется режим fallback;
    - добавлен флаг `bridge_health`;
    - исключения не скрываются полностью.

- [ ] **3.2 Добавить контракт-тесты API parity** (`TODO`)
  - Критерии:
    - Python-методы соответствуют Rust-экспортам;
    - fallback не ломает интерфейс.

---

## 🔥 PRIORITY 4 — CI & Workflow Hardening

- [ ] **4.1 Ограничить ruff autofix** (`TODO`)
  - Варианты выполнения:
    - убрать `--unsafe-fixes`,
    - или добавить approval-gate (`label: autofix-approved`).

- [ ] **4.2 Добавить `cargo clippy -- -D warnings`** (`TODO`)
  - Критерий:
    - предупреждения Rust переведены в критические.

---

## 🔥 PRIORITY 5 — Repository Hygiene

- [ ] **5.1 Синхронизировать веточную модель** (`TODO`)
  - Критерии:
    - восстановлен `main`;
    - CI привязан к `main`;
    - документация обновлена.

- [ ] **5.2 Добавить статус-бейджи** (`TODO`)
  - Область: `README.md`.
  - Критерии:
    - добавлены бейджи CI status, Python tests, Rust build.

---

## Готово к исполнению Codex

Codex может брать задачи из этого файла сверху вниз и выполнять их автоматически.
