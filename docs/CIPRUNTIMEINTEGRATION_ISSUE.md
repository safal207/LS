# CIP Runtime Integration Issue (Milestone)

## 🎯 Цель
Собрать минимально рабочий Web4 Runtime как связку RTT + CIP/HCP/LIP + ProtocolRouter + TrustFSM + AgentLoop + Observability, чтобы Codex мог автономно выполнять milestone от планирования до тестов и CI.

## 📦 Область работ

### Под‑issues
- RTT
- CIP
- HCP
- LIP
- ProtocolRouter
- TrustFSM
- AgentLoop
- Observability
- Tests
- CLI Tools

### Требуемые артефакты
- `execution-plan.md` в корне репозитория
- структура директорий под runtime‑подсистемы
- scaffolding кода (Python + Rust + pyo3)
- минимальные рабочие реализации:
  - RTT runtime
  - CIP runtime
  - HCP runtime
  - LIP runtime
  - ProtocolRouter
  - TrustFSM интеграция
  - AgentLoop интеграция
  - Observability слой
- интеграционные и unit‑тесты
- CI workflow (lint, type‑check, tests, Rust build, pyo3 build)
- CLI‑демо:
  - `scripts/web4_demo.py`
  - `scripts/web4_mesh_demo.py`

## ✅ Acceptance Criteria

- Все тесты проходят локально и в CI.
- RTT поддерживает reconnect + backpressure.
- CIP handshake детерминирован и покрыт тестами.
- TrustFSM покрыт тестами ≥ 90%.
- CLI‑демо запускаются без ошибок.
- `execution-plan.md` отражает фактически сделанное.

## 🚫 Out of Scope

- UI/GUI
- PKI/HSM
- масштабирование mesh > 10 узлов
- продвинутая оптимизация производительности

## 📌 Примечания

- Использовать существующие спецификации `docs/CIP_SPEC.md`, `docs/HCP_SPEC.md`, `docs/LIP_SPEC.md`.
- Реализации должны быть минимально рабочими, расширяемыми, и не ломать текущий функционал репозитория.
