[package]
name = "ncafuzzycore"
version = "0.1.0"
edition = "2021"

[lib]
name = "ncafuzzycore"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }

[dev-dependencies]
proptest = "1.5"
